import React from 'react';

const Canvas = () => {
    return (
        <div className={"canvas"}>
            <canvas>
                
            </canvas>
        </div>
    );
};

export default Canvas;