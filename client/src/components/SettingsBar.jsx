import React from 'react';
import '../styles/bar.sass'

const SettingsBar = () => {
    return (
        <div className={"bar"}>
            SettingsBar
        </div>
    );
};

export default SettingsBar;